name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: a-minimal-c-program
      run: cd a-minimal-c-program && sh build.sh
